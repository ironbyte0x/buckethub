# @buckethub/ui

A React component library for the BucketHub monorepo. Built on [@base-ui/react](https://base-ui.com/) primitives and styled with [Panda CSS](https://panda-css.com/) using a custom design system.

## Storybook

```bash
bun nx run ui:storybook
```
