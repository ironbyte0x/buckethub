import { styled } from '@buckethub/styled-system/jsx';

export const StyledBucketsList = styled('div', {
  base: {
    display: 'flex',
    flexDirection: 'column',
    gap: '2',
    paddingInline: '4',
    paddingBottom: '4'
  }
});
